# 📦 Order Management & Stock Report System

ระบบรายงานสต็อกสินค้า

## 🚀 วิธีการติดตั้งและเริ่มใช้งาน (Getting Started)

### 1) เตรียมความพร้อม (Prerequisites)
- ติดตั้ง **Docker**
- ติดตั้ง **Node.js** (ใช้สำหรับรันคำสั่ง Migration/Sync บนเครื่อง Developer)

### 2) รันระบบผ่าน Docker
ระบบถูกออกแบบให้ทำงานผ่าน **Docker Compose** ซึ่งประกอบด้วย 3 ส่วนหลัก: **Database**, **Backend**, และ **Frontend**

```bash
# รัน Container ทั้งหมดในโหมด Background
docker-compose up -d --build

# ตรวจสอบสถานะ Container
docker-compose ps
```

### 3) การจัดการฐานข้อมูล (Database Management & Seeding)
รันคำสั่งเหล่านี้ใน **Backend** ตามลำดับ เพื่อให้ฐานข้อมูลพร้อมใช้งาน:

```bash
# 1) เข้าไปที่โฟลเดอร์ backend
cd backend

# 2) สร้างไฟล์ Migration จาก Schema
npx drizzle-kit generate

# 3) นำ Migration ไปใช้กับฐานข้อมูล
npm run db:migrate

# 4) บังคับโครงสร้างให้ตรงกับ Schema (กรณี migrate ไม่ครอบคลุม)
npm run db:push

# 5) ดึงข้อมูลจาก External API เข้ามาเก็บลงตาราง (Sync/Seed ข้อมูล)
npm run db:sync
```

## 🏗️ การออกแบบระบบ (Architecture Design)

### 1) Database Design
ข้อมูลต้นทางจะมาจาก API 2 เส้น แต่เราออกแบบ database แยกเป็น 4 ตารางเพื่อประสิทธิภาพในการ Query และทำรายงาน:

- **categories**: เก็บหมวดหมู่หลัก (ใช้ทำ Filter ได้เร็ว)
- **subcategories**: เก็บหมวดหมู่ย่อย (เชื่อมกับรายการในออเดอร์)
- **orders**: เก็บข้อมูลหัวเอกสาร เช่น เลขที่/ประเภท/วันที่เวลา
- **order_items**: เก็บรายละเอียดสินค้าในแต่ละออเดอร์ (จำนวน, เกรด, ราคา, รวมเงิน)

### 2) Frontend & Deployment (SSR on EC2)
เหตุผลที่สร้าง **Frontend Image** เพื่อรองรับการ Deploy บน **AWS EC2** สำหรับ SSR:

- **Server-Side Rendering (SSR)**: การทำเป็น Image ช่วยให้รัน SSR ได้ลดปัญหา “หน้าขาว”/“Error บางหน้า” ที่อาจเกิดในบางรูปแบบการ deploy
- **Consistency**: มั่นใจว่า Environment บน EC2 ใกล้เคียงกับเครื่อง Developer มากที่สุด

## 🔍 ขั้นตอนการทำงานของ Filter & Search

1) **Frontend (React / shadcn/ui)**
- ผู้ใช้กรอกเงื่อนไข เช่น เกรด (A–D), หมวดหมู่, ช่วงวันที่
- Frontend ตรวจสอบความถูกต้อง (Validation)
- ส่ง HTTP GET Request พร้อม Query Parameters ไปยัง Backend

2) **Backend (Node.js / Drizzle ORM)**
- รับค่า Parameters แล้วทำการ Normalize/Clean ข้อมูล (เช่นตัวเลข)
- สร้าง Query ตามเงื่อนไขที่ส่งมา

3) **SQL Execution (Aggregated Logic)**
- **Filter (WHERE)**: กรองรายการให้ตรงเงื่อนไข (ช่องไหนไม่ส่งมาให้ถือว่าเป็นทั้งหมด)
- **Group & Aggregation**: จัดกลุ่มด้วย `sub_category_id` และคำนวณยอดซื้อ/ขายด้วย `SUM(...) FILTER (WHERE ...)`
- **Latest Record**: ใช้ `DISTINCT ON` เพื่อดึงวันที่/เลขที่ออเดอร์ล่าสุดของแต่ละสินค้า
- **Balance**: คำนวณ `stockBalance` และ `moneyBalance`
- **Pagination**: ใช้ `LIMIT` / `OFFSET` เพื่อให้โหลดเร็วและไม่ค้าง

4) **Output**
- Backend ส่ง JSON กลับไปที่ Frontend และแสดงผลในตารางด้วย shadcn/ui

## 🛠️ Tech Stack
- **Frontend**: Next.js, shadcn/ui, Tailwind CSS
- **Backend**: Node.js, TypeScript
- **Database**: PostgreSQL, Drizzle ORM
- **DevOps**: Docker, Docker Compose